from typing import List, Optional, Any
from parse_util import read_file, write_to_file
from langchain.text_splitter import LatexTextSplitter, Language

def langchain_ts():
#	filename = "main.tex"
#	file_contents = read_file(filename)

#	splitter = LatexTextSplitter(chunk_size = 512, chunk_overlap = 0)
		
#	split_data = splitter.split_text(file_contents)
#	print(split_data)		

	print("langchain stuff")
#langchain_ts()


